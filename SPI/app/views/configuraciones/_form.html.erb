<%= form_for(ponderacion) do |f| %>
  <% if ponderacion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ponderacion.errors.count, "error") %> prohibited this ponderacion from being saved:</h2>

      <ul>
      <% ponderacion.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :id_ponderacion %>
    <%= f.number_field :id_ponderacion %>
  </div>

  <div class="field">
    <%= f.label :segmento %>
    <%= f.number_field :segmento %>
  </div>

  <div class="field">
    <%= f.label :valor %>
    <%= f.text_field :valor %>
  </div>

  <div class="field">
    <%= f.label :fecha %>
    <%= f.date_field :fecha %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
  
<% end %>
