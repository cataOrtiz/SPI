<%= form_for(motivo) do |f| %>
  <% if motivo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(motivo.errors.count, "error") %> prohibited this motivo from being saved:</h2>

      <ul>
      <% motivo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :id_motivo %>
    <%= f.number_field :id_motivo %>
  </div>

  <div class="field">
    <%= f.label :tipo_motivo %>
    <%= f.text_field :tipo_motivo %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
